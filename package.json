{
  "name": "helm",
  "version": "0.1.0",
  "private": true,
  "description": "Intelligent Operational Control Layer for On-Chain Protocols — a GenLayer Intelligent Contract project.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "lint:contract": "genvm-lint check contracts/Helm.py",
    "test:direct": "gltest tests/direct",
    "deploy:helm": "tsx deploy/001_deploy_helm.ts"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
